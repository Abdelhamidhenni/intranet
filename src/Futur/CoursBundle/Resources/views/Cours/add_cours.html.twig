{% extends "FuturSiteBundle::layout.html.twig" %}

{% block main %}

<div class="addmod">

	<form method="post" {{ form_enctype(form) }}>
		 	{{ form_rest(form, { 'label': 'Enregistrer' }) }}
		 	<br/>
		    {{ form_widget(form, { 'label': 'Enregistrer' }) }}

		    <input type="submit" class="btn btn-primary" value="Enregistrer" />
	</form>

</div>


{% endblock %}
